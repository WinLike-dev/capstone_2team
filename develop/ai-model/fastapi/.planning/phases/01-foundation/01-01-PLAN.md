---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/__init__.py
  - app/main.py
  - app/core/__init__.py
  - app/core/config.py
  - app/core/lifespan.py
  - app/routers/__init__.py
  - app/services/__init__.py
  - app/clients/__init__.py
  - .env.example
  - .gitignore
  - requirements.txt
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "uvicorn 실행 후 GET /health 요청이 200 OK를 반환한다"
    - ".env 파일의 필수 환경변수가 누락되면 서버 시작 시 즉시 에러로 종료된다"
    - "lifespan 이벤트에서 외부 클라이언트 초기화 코드가 실행 위치에 배치되어 있다"
  artifacts:
    - path: "app/main.py"
      provides: "FastAPI 앱 인스턴스 + /health 엔드포인트"
      min_lines: 15
    - path: "app/core/config.py"
      provides: "pydantic-settings 기반 Settings 클래스"
      exports: ["Settings", "get_settings"]
    - path: "app/core/lifespan.py"
      provides: "lifespan 컨텍스트 매니저"
      exports: ["lifespan"]
    - path: ".env.example"
      provides: "필요한 환경변수 목록"
      min_lines: 4
  key_links:
    - from: "app/main.py"
      to: "app/core/lifespan.py"
      via: "lifespan parameter in FastAPI()"
      pattern: "FastAPI.*lifespan"
    - from: "app/main.py"
      to: "app/core/config.py"
      via: "Settings import for health check"
      pattern: "from app\\.core\\.config import"
    - from: "app/core/lifespan.py"
      to: "app/core/config.py"
      via: "Settings 로드 for client init"
      pattern: "get_settings|Settings"
---

<objective>
FastAPI 프로젝트 4계층 구조 생성, pydantic-settings 환경변수 관리, lifespan 이벤트 설정, /health 엔드포인트 구현.

Purpose: Phase 1의 서버 기반을 확립한다. 이후 모든 계층(schemas, routers, services, clients)이 이 구조 위에 빌드된다.
Output: 실행 가능한 FastAPI 서버 (GET /health 200 OK), 환경변수 검증, 4계층 디렉토리
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 프로젝트 구조 + Settings + lifespan + /health</name>
  <files>
    app/__init__.py,
    app/main.py,
    app/core/__init__.py,
    app/core/config.py,
    app/core/lifespan.py,
    app/routers/__init__.py,
    app/services/__init__.py,
    app/clients/__init__.py,
    .env.example,
    .gitignore,
    requirements.txt
  </files>
  <action>
1. **디렉토리 구조 생성** — 4계층 패키지:
   ```
   app/
     __init__.py
     main.py
     core/
       __init__.py
       config.py
       lifespan.py
     routers/
       __init__.py
     services/
       __init__.py
     clients/
       __init__.py
     schemas/          # Plan 02에서 파일 추가, 여기서는 빈 패키지만
       __init__.py
   ```
   모든 `__init__.py`는 빈 파일 (또는 `__all__` 정의만).

2. **app/core/config.py** — pydantic-settings BaseSettings 클래스:
   ```python
   from pydantic_settings import BaseSettings
   from functools import lru_cache

   class Settings(BaseSettings):
       # Gemini
       GEMINI_API_KEY: str
       # Pinecone
       PINECONE_API_KEY: str
       PINECONE_INDEX_NAME: str
       # WAS
       WAS_BASE_URL: str
       # App
       APP_ENV: str = "development"
       LOG_LEVEL: str = "INFO"

       model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}

   @lru_cache
   def get_settings() -> Settings:
       return Settings()
   ```
   필수 변수(GEMINI_API_KEY, PINECONE_API_KEY, PINECONE_INDEX_NAME, WAS_BASE_URL)는 기본값 없이 선언하여 누락 시 ValidationError 발생.

3. **app/core/lifespan.py** — asynccontextmanager 기반 lifespan:
   ```python
   from contextlib import asynccontextmanager
   from fastapi import FastAPI

   @asynccontextmanager
   async def lifespan(app: FastAPI):
       # Startup: Phase 2에서 Pinecone, httpx, 임베딩 모델 초기화 추가 예정
       # 현재는 placeholder 로그만
       import logging
       logger = logging.getLogger(__name__)
       logger.info("Starting up — client initialization placeholder")
       yield
       # Shutdown: Phase 2에서 cleanup 추가 예정
       logger.info("Shutting down — cleanup placeholder")
   ```

4. **app/main.py** — FastAPI 앱 생성 + /health:
   ```python
   from fastapi import FastAPI
   from app.core.lifespan import lifespan
   from app.core.config import get_settings

   app = FastAPI(title="AI Hub", lifespan=lifespan)

   @app.get("/health")
   async def health_check():
       settings = get_settings()
       return {"status": "ok", "env": settings.APP_ENV}
   ```
   Phase 2에서 router include 추가 예정.

5. **.env.example** — 필요 환경변수 목록:
   ```
   GEMINI_API_KEY=your-gemini-api-key
   PINECONE_API_KEY=your-pinecone-api-key
   PINECONE_INDEX_NAME=your-index-name
   WAS_BASE_URL=http://localhost:3000
   APP_ENV=development
   LOG_LEVEL=INFO
   ```

6. **.gitignore** — Python + .env:
   ```
   __pycache__/
   *.pyc
   .env
   .venv/
   venv/
   *.egg-info/
   dist/
   build/
   .pytest_cache/
   ```

7. **requirements.txt**:
   ```
   fastapi>=0.115.0
   uvicorn[standard]>=0.30.0
   pydantic>=2.0
   pydantic-settings>=2.0
   ```
   Phase 2에서 pinecone, google-genai, sentence-transformers, httpx 추가 예정.
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && pip install -r requirements.txt -q && python -c "from app.core.config import Settings; print('config OK')" 2>&1 | head -5</automated>
  </verify>
  <done>4계층 디렉토리 구조가 존재하고, Settings 클래스가 import 가능하다</done>
</task>

<task type="auto">
  <name>Task 2: 서버 기동 검증 + 환경변수 누락 검증</name>
  <files>tests/__init__.py, tests/test_health.py, .env</files>
  <action>
1. **테스트용 .env 생성** (gitignore 되어 있으므로 안전):
   ```
   GEMINI_API_KEY=test-key
   PINECONE_API_KEY=test-key
   PINECONE_INDEX_NAME=test-index
   WAS_BASE_URL=http://localhost:3000
   ```

2. **tests/test_health.py** — pytest + httpx AsyncClient:
   ```python
   import pytest
   from httpx import AsyncClient, ASGITransport
   from app.main import app

   @pytest.fixture
   def anyio_backend():
       return "asyncio"

   @pytest.mark.anyio
   async def test_health_returns_200():
       transport = ASGITransport(app=app)
       async with AsyncClient(transport=transport, base_url="http://test") as client:
           response = await client.get("/health")
       assert response.status_code == 200
       data = response.json()
       assert data["status"] == "ok"

   def test_settings_missing_env_raises(monkeypatch):
       """필수 환경변수 누락 시 ValidationError 발생 확인"""
       monkeypatch.delenv("GEMINI_API_KEY", raising=False)
       monkeypatch.delenv("PINECONE_API_KEY", raising=False)
       monkeypatch.delenv("PINECONE_INDEX_NAME", raising=False)
       monkeypatch.delenv("WAS_BASE_URL", raising=False)
       # lru_cache 초기화
       from app.core.config import get_settings
       get_settings.cache_clear()
       from pydantic import ValidationError
       with pytest.raises(ValidationError):
           from app.core.config import Settings
           Settings(_env_file=None)  # .env 파일도 무시
   ```

3. requirements.txt에 테스트 의존성 추가:
   ```
   pytest>=8.0
   anyio>=4.0
   httpx>=0.27.0
   pytest-anyio>=0.0.0
   ```
   (httpx는 이미 AsyncClient용으로 필요, pytest-anyio는 anyio marker 지원)
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && pip install -r requirements.txt -q && python -m pytest tests/test_health.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>GET /health가 200 + {"status": "ok"}을 반환하고, 필수 환경변수 누락 시 ValidationError가 발생한다</done>
</task>

</tasks>

<verification>
1. `uvicorn app.main:app --host 0.0.0.0 --port 8000` 실행 후 `curl http://localhost:8000/health` → 200 OK
2. `.env` 없이 서버 시작 → pydantic ValidationError로 즉시 종료
3. `python -m pytest tests/test_health.py -v` → 2 tests passed
</verification>

<success_criteria>
- FastAPI 서버가 /health에 200 OK 응답
- 4계층 디렉토리 구조 (routers/services/clients/core) 존재
- pydantic-settings로 환경변수 검증, 누락 시 시작 실패
- lifespan 이벤트에 클라이언트 초기화 placeholder 존재
- 테스트 2개 통과
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
