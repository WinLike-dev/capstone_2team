---
phase: 02-core-integrations
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/prompts/router.py
  - app/clients/router.py
  - tests/test_router.py
autonomous: true
requirements: [ROUT-01, ROUT-02, ROUT-03]

must_haves:
  truths:
    - "RouterClient.classify()가 사용자 메시지를 받아 mode(1-6)와 reason을 JSON으로 반환한다"
    - "JSON 파싱 실패 시 mode=1(단순대화)로 폴백한다"
    - "RouterClient가 GeminiClient와 독립적으로 테스트 가능하다"
  artifacts:
    - path: "app/clients/router.py"
      provides: "RouterClient with classify() method"
      exports: ["RouterClient", "RouterOutput"]
    - path: "app/prompts/router.py"
      provides: "ROUTER_SYSTEM_PROMPT constant"
      contains: "ROUTER_SYSTEM_PROMPT"
    - path: "tests/test_router.py"
      provides: "Unit tests with mocked genai client"
      min_lines: 25
  key_links:
    - from: "app/clients/router.py"
      to: "google.genai.Client.aio.models.generate_content"
      via: "Gemini Flash Lite API call"
      pattern: "self._client\\.aio\\.models\\.generate_content"
    - from: "app/clients/router.py"
      to: "app/prompts/router.py"
      via: "import ROUTER_SYSTEM_PROMPT"
      pattern: "from app\\.prompts\\.router import"
---

<objective>
Router AI 클라이언트 + 시스템 프롬프트 구현

Purpose: Gemini Flash Lite 기반 RouterClient를 구현하여 사용자 메시지의 의도를 6가지 모드 중 하나로 분류한다. 별도 API 키(ROUTER_API_KEY)를 사용하며, GeminiClient와 독립적으로 동작한다. v2(/ai-chat)에서 사용될 예정이지만 클라이언트는 이 Phase에서 완성한다.

Output: RouterClient 클래스, Router 시스템 프롬프트, 단위 테스트
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-integrations/02-CONTEXT.md
@.planning/phases/02-core-integrations/02-RESEARCH.md

<interfaces>
<!-- Router system instruction reference -->
<!-- From CONTEXT.md: docs/router_system_instruction.txt 내용을 app/prompts/router.py로 Python 상수로 이전 -->
<!-- 6가지 모드: 1=단순대화, 2=플랜작성, 3=플랜수정, 4=식단작성, 5=식단수정, 6=DB수정 -->
<!-- 출력: {mode: int, reason: str} — confidence score 없음 (CONTEXT.md 결정) -->

Router output schema (from Research Pattern 4):
```python
class RouterOutput(BaseModel):
    mode: int  # 1-6
    reason: str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router 시스템 프롬프트 + RouterClient 구현</name>
  <files>app/prompts/router.py, app/clients/router.py</files>
  <action>
1. app/prompts/router.py 생성 (ROUT-02):
   - ROUTER_SYSTEM_PROMPT: str 상수 정의
   - 프롬프트 내용 (docs/router_system_instruction.txt 기반으로 작성):
     - "당신은 사용자 메시지의 의도를 분류하는 라우터입니다."
     - 6가지 모드 정의:
       - mode 1: 단순 대화 (일상 대화, 인사, 잡담)
       - mode 2: 운동 플랜 작성 (새 운동 계획 요청)
       - mode 3: 운동 플랜 수정 (기존 운동 계획 변경)
       - mode 4: 식단 작성 (새 식단 계획 요청)
       - mode 5: 식단 수정 (기존 식단 변경)
       - mode 6: DB 수정 (사용자 정보 업데이트 요청)
     - "반드시 JSON 형식으로만 응답하세요: {\"mode\": <1-6 정수>, \"reason\": \"<분류 이유>\"}"
     - "확실하지 않으면 mode 1로 분류하세요."

   NOTE: docs/router_system_instruction.txt 파일이 존재하면 그 내용을 참조하여 프롬프트를 작성한다. 파일이 없으면 위 내용으로 자체 작성한다.

2. app/clients/router.py 생성 (ROUT-01, ROUT-03):
   - RouterOutput(BaseModel): mode: int, reason: str
   - RouterClient 클래스 (GeminiClient와 완전 분리)
   - __init__(self, api_key: str, model_name: str) -> None:
     - self._client = genai.Client(api_key=api_key)
     - self._model_name = model_name
   - async def classify(self, user_message: str) -> RouterOutput:
     - response = await self._client.aio.models.generate_content(model=self._model_name, contents=user_message, config=types.GenerateContentConfig(system_instruction=ROUTER_SYSTEM_PROMPT, response_mime_type="application/json", response_schema=RouterOutput))
     - try: return RouterOutput.model_validate_json(response.text)
     - except Exception: return RouterOutput(mode=1, reason="파싱 실패 - 기본 모드로 처리")

CRITICAL: RouterClient는 별도 API 키(ROUTER_API_KEY)와 별도 모델(ROUTER_MODEL_NAME)을 사용한다 (CONTEXT.md 결정).
CRITICAL: 출력은 mode + reason만. confidence score 없음 (CONTEXT.md 결정).
CRITICAL: JSON 파싱 실패 시 mode=1로 폴백 (Research Pattern 4).
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -c "from app.clients.router import RouterClient, RouterOutput; from app.prompts.router import ROUTER_SYSTEM_PROMPT; print('Import OK, prompt length:', len(ROUTER_SYSTEM_PROMPT))"</automated>
  </verify>
  <done>RouterClient와 RouterOutput이 import 가능하고 ROUTER_SYSTEM_PROMPT가 비어있지 않다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RouterClient 단위 테스트 (모킹)</name>
  <files>tests/test_router.py</files>
  <behavior>
    - Test 1: classify("오늘 뭐 먹지?")가 mode=4 (식단 작성) 등 올바른 RouterOutput을 반환 (모킹된 응답)
    - Test 2: classify()가 Gemini API를 ROUTER_SYSTEM_PROMPT와 함께 호출
    - Test 3: Gemini가 잘못된 JSON을 반환하면 mode=1로 폴백
    - Test 4: Gemini가 빈 응답을 반환하면 mode=1로 폴백
    - Test 5: RouterOutput의 mode가 1-6 범위인지 검증 (정상 응답)
  </behavior>
  <action>
1. tests/test_router.py 생성:
   - genai.Client를 MagicMock으로 모킹
   - 정상 케이스: generate_content가 '{"mode": 4, "reason": "식단 관련 요청"}' 반환하도록 설정
   - 파싱 실패 케이스: generate_content가 'invalid json' 반환 → mode=1 폴백 확인
   - 빈 응답 케이스: generate_content가 '' 반환 → mode=1 폴백 확인
   - system_instruction 검증: call_args에서 config.system_instruction이 ROUTER_SYSTEM_PROMPT인지 확인
   - @pytest.mark.anyio 데코레이터 사용
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_router.py -v</automated>
  </verify>
  <done>5개 이상 테스트 통과. classify() 정상 호출, 폴백 동작, 시스템 프롬프트 전달이 모두 검증됨.</done>
</task>

</tasks>

<verification>
- RouterClient.classify()가 RouterOutput(mode, reason)을 반환
- JSON 파싱 실패 시 mode=1 폴백
- 별도 API 키/모델 사용 (GeminiClient와 독립)
- 모든 테스트 통과: python -m pytest tests/test_router.py -v
</verification>

<success_criteria>
- RouterClient가 async classify() 메서드 제공
- 6가지 모드(1-6) 분류 가능
- JSON 파싱 실패 시 안전한 폴백
- GeminiClient와 독립적으로 테스트 가능
- 5개 이상 단위 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-integrations/02-04-SUMMARY.md`
</output>
