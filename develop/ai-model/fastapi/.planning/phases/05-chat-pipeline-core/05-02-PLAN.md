---
phase: 05-chat-pipeline-core
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/routers/chat.py
  - app/main.py
  - tests/test_chat_router.py
autonomous: true
requirements: [CHAT-01]

must_haves:
  truths:
    - "POST /ai-chat 요청이 AiChatRequest를 받아 AiChatResponse를 반환한다"
    - "POST /ai-chat 엔드포인트가 main.py에 등록되어 있다"
    - "잘못된 요청 바디가 422 구조화 에러로 반환된다"
  artifacts:
    - path: "app/routers/chat.py"
      provides: "POST /ai-chat endpoint"
      exports: ["router"]
    - path: "tests/test_chat_router.py"
      provides: "Chat router integration tests"
      min_lines: 30
  key_links:
    - from: "app/routers/chat.py"
      to: "app/services/chat_service.py"
      via: "handle_ai_chat()"
      pattern: "handle_ai_chat"
    - from: "app/main.py"
      to: "app/routers/chat.py"
      via: "app.include_router(chat_router)"
      pattern: "include_router.*chat"
---

<objective>
POST /ai-chat 엔드포인트를 생성하고 main.py에 등록하여 Chat Pipeline을 외부에서 호출 가능하게 한다.

Purpose: Phase 5의 마지막 단계 — 서비스 로직(05-01)을 HTTP 엔드포인트로 노출
Output: app/routers/chat.py, tests/test_chat_router.py, updated app/main.py
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-chat-pipeline-core/05-01-SUMMARY.md

<interfaces>
<!-- From app/services/chat_service.py (created in 05-01) -->
```python
async def handle_ai_chat(
    body: AiChatRequest,
    request: Request,
    background_tasks: BackgroundTasks,
) -> AiChatResponse: ...
```

<!-- From app/schemas/chat.py -->
```python
class AiChatRequest(BaseModel):
    user_id: str
    user_profile: UserProfile
    user_instruction: str = ""
    user_message: str

class AiChatResponse(BaseModel):
    status: str = "success"
    mode: int
    data: AiChatData
    db_modified_flag: DbModifiedFlag
```

<!-- Existing router pattern from app/routers/meal.py -->
```python
from fastapi import APIRouter, BackgroundTasks, Request

router = APIRouter(tags=["meal"])

@router.post("/process-meal", response_model=SuccessResponse)
async def process_meal_endpoint(
    body: ProcessMealRequest,
    request: Request,
    background_tasks: BackgroundTasks,
) -> SuccessResponse:
    return await handle_process_meal(body, request, background_tasks)
```

<!-- Existing main.py registration pattern -->
```python
from app.routers.meal import router as meal_router
app.include_router(meal_router)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /ai-chat 라우터 + main.py 등록</name>
  <files>app/routers/chat.py, app/main.py, tests/test_chat_router.py</files>
  <behavior>
    - Test 1: POST /ai-chat with valid body returns 200 + AiChatResponse JSON (mock handle_ai_chat)
    - Test 2: POST /ai-chat with missing user_message returns 422 structured error
    - Test 3: POST /ai-chat with missing user_id returns 422 structured error
    - Test 4: Response contains mode, data.message, db_modified_flag fields
    - Test 5: handle_ai_chat 내부 에러 시 500 structured error 반환
  </behavior>
  <action>
**app/routers/chat.py:**

기존 `app/routers/meal.py` 패턴을 따른다:

```python
from fastapi import APIRouter, BackgroundTasks, Request
from app.schemas.chat import AiChatRequest, AiChatResponse
from app.services.chat_service import handle_ai_chat

router = APIRouter(tags=["chat"])

@router.post("/ai-chat", response_model=AiChatResponse)
async def ai_chat_endpoint(
    body: AiChatRequest,
    request: Request,
    background_tasks: BackgroundTasks,
) -> AiChatResponse:
    """AI 채팅 파이프라인을 실행한다. Router AI 의도분류 -> 병렬 맥락검색 -> 워커 AI 응답."""
    return await handle_ai_chat(body, request, background_tasks)
```

**app/main.py 수정:**

기존 import/include_router 패턴 따름:
```python
from app.routers.chat import router as chat_router
# ...
app.include_router(chat_router)
```

meal_router, recommend_router 뒤에 추가한다.

**tests/test_chat_router.py:**

httpx.AsyncClient + ASGITransport로 테스트 (TestClient 대신 async).
`pytestmark = pytest.mark.anyio` 사용.

handle_ai_chat를 `unittest.mock.AsyncMock`으로 패치하여 라우터 계층만 테스트한다.
mock은 `app.services.chat_service.handle_ai_chat`을 패치 대상으로 한다.

Valid request body:
```python
{
    "user_id": "test-user-1",
    "user_profile": {"gender": "male", "age": 25},
    "user_instruction": "",
    "user_message": "안녕하세요"
}
```

Mock return value:
```python
AiChatResponse(
    mode=1,
    data=AiChatData(message="안녕하세요! 무엇을 도와드릴까요?"),
    db_modified_flag="none",
)
```
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_chat_router.py -x -v</automated>
  </verify>
  <done>POST /ai-chat가 AiChatRequest를 받아 AiChatResponse를 반환한다. 유효하지 않은 요청은 422 구조화 에러를 반환한다. 5개 이상의 테스트가 통과한다.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_chat_router.py tests/test_chat_service.py -x -v` — 전체 통과
- `python -c "from app.routers.chat import router"` — import 성공
- `grep -n 'chat_router' app/main.py` — 등록 확인
</verification>

<success_criteria>
- POST /ai-chat 엔드포인트가 존재하고 AiChatResponse를 반환한다
- 잘못된 요청에 대해 422 구조화 에러가 반환된다
- main.py에 chat_router가 등록되어 있다
- 전체 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/05-chat-pipeline-core/05-02-SUMMARY.md`
</output>
