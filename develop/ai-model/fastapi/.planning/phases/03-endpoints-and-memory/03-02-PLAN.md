---
phase: 03-endpoints-and-memory
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/services/meal_service.py
  - app/routers/meal.py
  - app/main.py
  - tests/test_meal_service.py
  - tests/test_endpoints.py
autonomous: true
requirements: [MEAL-01, MEAL-02, MEAL-03, MEAL-04, MEAL-05]

must_haves:
  truths:
    - "POST /process-meal에 유효한 요청을 보내면 {status: success, data: {calories, message}} 형식 응답이 반환된다"
    - "서비스가 Pinecone에서 사용자 맥락을 검색하고 프롬프트에 주입한다"
    - "Pinecone 검색 실패 시 맥락 없이 정상 응답을 반환한다"
    - "Gemini 실패(tenacity 재시도 후) 시 500 + GEMINI_ERROR 코드를 반환한다"
    - "응답 반환 후 BackgroundTasks로 run_background_summary가 등록된다"
    - "예상치 못한 예외 시 글로벌 핸들러가 500 + INTERNAL_ERROR를 반환한다"
  artifacts:
    - path: "app/services/meal_service.py"
      provides: "process_meal() 파이프라인 오케스트레이터"
      exports: ["process_meal"]
    - path: "app/routers/meal.py"
      provides: "POST /process-meal (stub -> service 호출 교체)"
      contains: "from app.services.meal_service import process_meal"
    - path: "app/main.py"
      provides: "글로벌 exception_handler 등록"
      contains: "exception_handler"
  key_links:
    - from: "app/routers/meal.py"
      to: "app/services/meal_service.py"
      via: "await process_meal(body, request, background_tasks)"
      pattern: "await process_meal"
    - from: "app/services/meal_service.py"
      to: "request.app.state"
      via: "클라이언트 인스턴스 접근"
      pattern: "request\\.app\\.state"
    - from: "app/services/meal_service.py"
      to: "app/services/background_summary.py"
      via: "background_tasks.add_task(run_background_summary, ...)"
      pattern: "add_task.*run_background_summary"
    - from: "app/services/meal_service.py"
      to: "app/prompts/meal.py"
      via: "build_meal_system_prompt(profile, context_text)"
      pattern: "build_meal_system_prompt"
---

<objective>
/process-meal 엔드포인트를 stub에서 실제 서비스 파이프라인으로 교체하고, 글로벌 예외 핸들러를 main.py에 등록한다.

Purpose: WAS가 POST /process-meal을 호출하면 Pinecone 맥락 검색 -> Gemini 식단 분석 -> 응답 반환 -> Background Summary 저장 전체 파이프라인이 동작한다.
Output: app/services/meal_service.py, 수정된 app/routers/meal.py, 글로벌 핸들러가 추가된 app/main.py, 유닛/통합 테스트
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endpoints-and-memory/03-CONTEXT.md
@.planning/phases/03-endpoints-and-memory/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 인터페이스 -->

From app/services/background_summary.py (Plan 01 생성):
```python
async def run_background_summary(
    user_id: str,
    user_message: str,
    ai_response: str,
    gemini_client,
    embed_client,
    pinecone_client,
) -> None:
```

From app/prompts/meal.py (Plan 01 확장):
```python
def build_meal_system_prompt(user_profile: UserProfile, context_text: str = "이전 맥락: 없음") -> str:
```

From app/prompts/summary.py (Plan 01 생성):
```python
class SummaryOutput(BaseModel):
    summary: str
def build_summary_prompt() -> str:
```

<!-- Phase 2 클라이언트 인터페이스 -->

From app/clients/gemini.py:
```python
class GeminiClient:
    async def generate(self, system_prompt: str, user_content: str, response_schema: type) -> str:
```

From app/clients/embedding.py:
```python
class EmbeddingClient:
    async def embed(self, text: str) -> list[float]:
```

From app/clients/pinecone.py:
```python
class PineconeClient:
    async def upsert(self, user_id: str, vector: list[float], summary: str) -> str:
    async def search(self, user_id: str, vector: list[float], top_k: int = 3) -> list[dict]:
```

From app/schemas/meal.py:
```python
class ProcessMealRequest(BaseModel):
    user_id: str
    user_profile: UserProfile
    user_instruction: str
    user_message: str

class MealAnalysisData(BaseModel):
    calories: float
    message: str
```

From app/schemas/common.py:
```python
class SuccessResponse(BaseModel):
    status: str = "success"
    data: Any

class ErrorResponse(BaseModel):
    status: str = "error"
    error: ErrorDetail

class ErrorDetail(BaseModel):
    code: str
    message: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: meal_service.py 구현 + 유닛 테스트</name>
  <files>app/services/meal_service.py, tests/test_meal_service.py</files>
  <behavior>
    - test_process_meal_passes_fields: body의 user_id, user_message가 올바르게 사용됨
    - test_process_meal_pinecone_search: embed.embed(user_message) -> pinecone.search(user_id, vector, top_k=3) 순서로 호출
    - test_process_meal_pinecone_failure: pinecone.search가 Exception raise해도 process_meal은 정상 응답 반환 (graceful degradation)
    - test_process_meal_context_injection: 검색 결과가 있으면 "이전 맥락:\n1. ..." 형식으로 build_meal_system_prompt에 전달
    - test_process_meal_no_context: 검색 결과 빈 리스트일 때 "이전 맥락: 없음" 전달
    - test_process_meal_gemini_call: gemini.generate(system_prompt, user_message, MealAnalysisData) 호출 확인
    - test_process_meal_response_format: 반환값이 SuccessResponse(data={calories: float, message: str})
    - test_process_meal_gemini_failure: gemini.generate가 ClientError raise 시 HTTPException(500) with GEMINI_ERROR
    - test_background_task_registered: background_tasks.add_task(run_background_summary, ...) 호출 확인
  </behavior>
  <action>
    1. app/services/meal_service.py 생성:
       - process_meal(body: ProcessMealRequest, request: Request, background_tasks: BackgroundTasks) -> SuccessResponse
       - 클라이언트 접근: gemini = request.app.state.gemini_client, pinecone = request.app.state.pinecone_client, embed = request.app.state.embed_client
       - 내부 헬퍼 _fetch_context(pinecone, embed, user_id, query) -> str:
         - try: embed.embed(query) -> pinecone.search(user_id, vector, top_k=3) -> 결과 포매팅
         - 결과 없으면 "이전 맥락: 없음"
         - except Exception: logger.warning() + "이전 맥락: 없음" 반환
       - 파이프라인: _fetch_context -> build_meal_system_prompt(body.user_profile, context_text) -> gemini.generate(prompt, body.user_message, MealAnalysisData)
       - Gemini 실패 처리: try/except genai_errors.ClientError -> raise HTTPException(status_code=500, detail={"status": "error", "error": {"code": "GEMINI_ERROR", "message": str(exc)}})
       - 응답 파싱: json.loads(raw_json) -> MealAnalysisData(**parsed)
       - Background Summary 등록: background_tasks.add_task(run_background_summary, user_id=body.user_id, user_message=body.user_message, ai_response=data.message, gemini_client=gemini, embed_client=embed, pinecone_client=pinecone)
       - return SuccessResponse(data=data.model_dump())

    2. tests/test_meal_service.py 생성:
       - mock request.app.state에 AsyncMock 클라이언트 3개 설정
       - mock background_tasks = MagicMock(spec=BackgroundTasks)
       - gemini_client.generate 반환값: '{"calories": 350.0, "message": "균형 잡힌 식사"}'
       - embed_client.embed 반환값: [0.1]*384
       - pinecone_client.search 반환값: [{"id": "1", "score": 0.9, "summary": "어제 치킨", "timestamp": "..."}]
       - pytest.mark.anyio 사용
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_meal_service.py -x -v</automated>
  </verify>
  <done>
    - process_meal()이 전체 파이프라인(맥락검색 -> 프롬프트빌드 -> Gemini호출 -> 응답직렬화 -> BackgroundTask등록)을 실행
    - Pinecone 실패 시 graceful degradation
    - Gemini 실패 시 HTTPException(500, GEMINI_ERROR)
    - 9개 테스트 모두 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 라우터 교체 + 글로벌 예외 핸들러 + 통합 테스트</name>
  <files>app/routers/meal.py, app/main.py, tests/test_endpoints.py</files>
  <action>
    1. app/routers/meal.py 수정:
       - stub body 제거
       - from fastapi import APIRouter, BackgroundTasks, Request 추가
       - from app.services.meal_service import process_meal as handle_process_meal
       - process_meal_endpoint(body: ProcessMealRequest, request: Request, background_tasks: BackgroundTasks) -> SuccessResponse
       - return await handle_process_meal(body, request, background_tasks)

    2. app/main.py 수정:
       - import logging, traceback, from fastapi import Request as FastAPIRequest, from fastapi.responses import JSONResponse
       - @app.exception_handler(Exception) 데코레이터로 global_exception_handler 등록
       - logger.error("Unhandled exception: %s\n%s", str(exc), traceback.format_exc())
       - return JSONResponse(status_code=500, content={"status": "error", "error": {"code": "INTERNAL_ERROR", "message": "내부 서버 오류가 발생했습니다."}})

    3. tests/test_endpoints.py 생성 (또는 기존 test_stub_endpoints.py와 분리):
       - test_process_meal_calls_service: TestClient로 POST /process-meal 호출, 서비스 레이어를 mock하여 200 응답 확인
       - test_process_meal_422_invalid_body: 필수 필드 누락 시 422 반환 (글로벌 핸들러가 가로채지 않는지 확인)
       - test_global_handler_500: 서비스에서 예상치 못한 Exception raise 시 500 + INTERNAL_ERROR 반환
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_endpoints.py tests/test_meal_service.py -x -v</automated>
  </verify>
  <done>
    - POST /process-meal이 stub 대신 실제 서비스를 호출
    - 잘못된 요청 시 422 반환 (글로벌 핸들러 미간섭)
    - 예상치 못한 예외 시 500 + INTERNAL_ERROR 반환
    - 기존 /health 엔드포인트 정상 동작
  </done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_meal_service.py tests/test_endpoints.py -x -v 전체 통과
- python -m pytest tests/ -x 로 기존 테스트 포함 전체 통과
- POST /process-meal이 stub이 아닌 서비스 파이프라인을 호출
</verification>

<success_criteria>
- POST /process-meal에 유효한 요청 시 {status: "success", data: {calories, message}} 반환
- Pinecone 검색 실패 시에도 정상 응답 반환
- Gemini 실패 시 500 + GEMINI_ERROR 반환
- 예상치 못한 예외 시 500 + INTERNAL_ERROR 반환 (글로벌 핸들러)
- 응답 후 Background Summary가 BackgroundTasks에 등록됨
</success_criteria>

<output>
After completion, create `.planning/phases/03-endpoints-and-memory/03-02-SUMMARY.md`
</output>
