---
phase: 03-endpoints-and-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/prompts/summary.py
  - app/prompts/meal.py
  - app/prompts/recommend.py
  - app/services/background_summary.py
  - tests/test_background_summary.py
  - tests/test_prompts.py
autonomous: true
requirements: [BGSM-01, BGSM-02, BGSM-03, BGSM-04, BGSM-05]

must_haves:
  truths:
    - "Background Summary가 user_message + ai_response를 Gemini로 요약한다"
    - "요약 텍스트가 임베딩으로 변환되어 Pinecone에 저장된다"
    - "Background Summary 실패 시 예외가 전파되지 않고 로그만 남는다"
    - "프롬프트 빌더에 context_text 파라미터가 추가되어 맥락 주입이 가능하다"
  artifacts:
    - path: "app/prompts/summary.py"
      provides: "SummaryOutput 스키마 + build_summary_prompt()"
      exports: ["SummaryOutput", "build_summary_prompt"]
    - path: "app/services/background_summary.py"
      provides: "run_background_summary() 비동기 파이프라인"
      exports: ["run_background_summary"]
    - path: "app/prompts/meal.py"
      provides: "build_meal_system_prompt(user_profile, context_text) 확장"
      contains: "context_text"
    - path: "app/prompts/recommend.py"
      provides: "build_recommend_system_prompt(user_profile, context_text) 확장"
      contains: "context_text"
  key_links:
    - from: "app/services/background_summary.py"
      to: "app/prompts/summary.py"
      via: "import build_summary_prompt, SummaryOutput"
      pattern: "from app\\.prompts\\.summary import"
    - from: "app/services/background_summary.py"
      to: "gemini_client.generate()"
      via: "Gemini 호출로 요약 생성"
      pattern: "gemini_client\\.generate"
    - from: "app/services/background_summary.py"
      to: "embed_client.embed()"
      via: "요약 텍스트 임베딩"
      pattern: "embed_client\\.embed"
    - from: "app/services/background_summary.py"
      to: "pinecone_client.upsert()"
      via: "벡터 저장"
      pattern: "pinecone_client\\.upsert"
---

<objective>
Background Summary 파이프라인과 프롬프트 확장을 구현한다. Plan 02, 03의 서비스 레이어가 의존하는 공용 인프라를 먼저 완성한다.

Purpose: 두 엔드포인트(process-meal, recommend) 모두 (1) 맥락 주입된 프롬프트와 (2) 응답 후 비동기 요약 저장을 사용한다. 이 공용 컴포넌트를 먼저 만들어야 서비스 레이어 구현이 가능하다.
Output: app/prompts/summary.py, app/services/background_summary.py, 확장된 prompt builders, 유닛 테스트
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endpoints-and-memory/03-CONTEXT.md

<interfaces>
<!-- Phase 2 client interfaces used by background_summary.py -->

From app/clients/gemini.py:
```python
class GeminiClient:
    async def generate(self, system_prompt: str, user_content: str, response_schema: type) -> str:
        """Returns JSON text from Gemini API."""
```

From app/clients/embedding.py:
```python
EMBEDDING_DIM: int = 384

class EmbeddingClient:
    async def embed(self, text: str) -> list[float]:
        """Return a 384-dim float vector for text."""
```

From app/clients/pinecone.py:
```python
class PineconeClient:
    async def upsert(self, user_id: str, vector: list[float], summary: str) -> str:
        """벡터를 user_id namespace에 저장하고 생성된 ID를 반환."""
    async def search(self, user_id: str, vector: list[float], top_k: int = 3) -> list[dict]:
        """[{id, score, summary, timestamp}] 형태의 dict 리스트 반환."""
```

From app/prompts/meal.py (현재 시그니처):
```python
def build_meal_system_prompt(user_profile: UserProfile) -> str:
```

From app/prompts/recommend.py (현재 시그니처):
```python
def build_recommend_system_prompt(user_profile: UserProfile) -> str:
```

From app/schemas/common.py:
```python
class UserProfile(BaseModel):
    gender: Optional[str] = None
    age: Optional[int] = None
    bmi: Optional[float] = None
    goal: Optional[str] = None
    medical_history: Optional[list[str]] = None
    allergies: Optional[list[str]] = None
    activity_level: Optional[str] = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Summary 프롬프트 + Background Summary 파이프라인 + 테스트</name>
  <files>app/prompts/summary.py, app/services/background_summary.py, tests/test_background_summary.py</files>
  <behavior>
    - test_summary_gemini_call: run_background_summary가 gemini_client.generate()를 build_summary_prompt() + "질문: {user_message}\n답변: {ai_response}" 인자로 호출한다
    - test_summary_embed_call: Gemini 요약 결과를 embed_client.embed()에 전달한다
    - test_summary_pinecone_upsert: embed 결과 벡터를 pinecone_client.upsert(user_id=user_id, vector=vector, summary=summary_text)로 저장한다
    - test_summary_error_silent: gemini_client.generate()가 Exception을 raise해도 run_background_summary가 예외를 전파하지 않는다
    - test_summary_error_logging: 실패 시 logger.exception()이 호출된다 (caplog 사용)
  </behavior>
  <action>
    1. app/prompts/summary.py 생성:
       - SummaryOutput(BaseModel): summary: str (non-Optional, Pitfall 3 방지)
       - SUMMARY_SYSTEM_PROMPT 상수: "당신은 대화 요약 전문가입니다.\n주어진 질문과 답변을 2-3문장으로 간결하게 요약하세요.\n한국어로 응답하세요."
       - build_summary_prompt() -> str: SUMMARY_SYSTEM_PROMPT 반환

    2. app/services/background_summary.py 생성:
       - run_background_summary(user_id, user_message, ai_response, gemini_client, embed_client, pinecone_client) -> None
       - 파이프라인: build_summary_prompt() -> gemini.generate(prompt, content, SummaryOutput) -> json.loads -> embed(summary_text) -> pinecone.upsert(user_id, vector, summary_text)
       - 전체를 try/except Exception으로 감싸고, except에서 logger.exception() 호출. 절대 re-raise 하지 않음 (BGSM-05)
       - Background task 함수 시그니처에 request: Request를 넣지 않음 (Pitfall 1 방지). 클라이언트 인스턴스를 직접 인자로 받음

    3. tests/test_background_summary.py 생성:
       - 모든 클라이언트를 AsyncMock으로 mock
       - gemini_client.generate는 '{"summary": "테스트 요약"}' 반환
       - embed_client.embed는 [0.1]*384 반환
       - pinecone_client.upsert는 "test-id" 반환
       - pytest.mark.anyio 사용
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_background_summary.py -x -v</automated>
  </verify>
  <done>
    - SummaryOutput 스키마가 summary: str 필드 1개로 존재
    - run_background_summary()가 전체 파이프라인(요약 -> 임베딩 -> 저장)을 실행
    - 에러 발생 시 로그만 남기고 예외 전파 없음
    - 5개 테스트 모두 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 프롬프트 빌더 context_text 파라미터 확장</name>
  <files>app/prompts/meal.py, app/prompts/recommend.py, tests/test_prompts.py</files>
  <action>
    1. app/prompts/meal.py 수정:
       - build_meal_system_prompt(user_profile: UserProfile, context_text: str = "이전 맥락: 없음") -> str
       - 기존 프롬프트 문자열 끝 "한국어로 응답하세요." 앞에 f"{context_text}\n" 삽입
       - 기본값이 있으므로 기존 Phase 2 테스트는 변경 불필요 (Pitfall 5 방지)

    2. app/prompts/recommend.py 수정:
       - build_recommend_system_prompt(user_profile: UserProfile, context_text: str = "이전 맥락: 없음") -> str
       - 동일 패턴으로 context_text 삽입

    3. tests/test_prompts.py 생성 (또는 기존에 있으면 추가):
       - test_meal_prompt_with_context: context_text="이전 맥락:\n1. 어제 치킨 먹음" 전달 시 반환 문자열에 해당 내용 포함 확인
       - test_meal_prompt_default_context: 인자 없이 호출 시 "이전 맥락: 없음" 포함 확인
       - test_recommend_prompt_with_context: 동일 패턴
       - test_recommend_prompt_default_context: 동일 패턴
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_prompts.py tests/test_background_summary.py -x -v</automated>
  </verify>
  <done>
    - build_meal_system_prompt()와 build_recommend_system_prompt()가 context_text 파라미터 수용
    - 기본값 "이전 맥락: 없음"으로 기존 호출 호환성 유지
    - 맥락 텍스트가 시스템 프롬프트에 포함됨
    - 4개 프롬프트 테스트 + 5개 background summary 테스트 통과
  </done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_background_summary.py tests/test_prompts.py -x -v 전체 통과
- app/prompts/summary.py에서 SummaryOutput, build_summary_prompt import 가능
- app/services/background_summary.py에서 run_background_summary import 가능
- 기존 Phase 2 테스트 (tests/test_gemini.py, tests/test_schemas.py 등) 깨지지 않음: python -m pytest tests/ -x
</verification>

<success_criteria>
- run_background_summary()가 Gemini 요약 -> 임베딩 -> Pinecone 저장 파이프라인을 실행한다
- 파이프라인 에러 시 로그만 남기고 예외를 전파하지 않는다
- 프롬프트 빌더가 context_text를 시스템 프롬프트에 포함한다
- 기존 테스트가 모두 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/03-endpoints-and-memory/03-01-SUMMARY.md`
</output>
