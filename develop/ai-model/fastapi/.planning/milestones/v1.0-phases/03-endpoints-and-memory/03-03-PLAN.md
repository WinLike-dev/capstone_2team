---
phase: 03-endpoints-and-memory
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/services/recommend_service.py
  - app/routers/recommend.py
  - tests/test_recommend_service.py
autonomous: true
requirements: [RECOM-01, RECOM-02, RECOM-03, RECOM-04, RECOM-05]

must_haves:
  truths:
    - "POST /recommend에 유효한 요청을 보내면 {status: success, data: {recommended_exercise, recommended_meal}} 형식 응답이 반환된다"
    - "서비스가 Pinecone에서 사용자 맥락을 검색하고 프롬프트에 주입한다"
    - "Pinecone 검색 실패 시 맥락 없이 정상 응답을 반환한다"
    - "Gemini 실패 시 500 + GEMINI_ERROR 코드를 반환한다"
    - "응답 반환 후 BackgroundTasks로 run_background_summary가 등록된다"
  artifacts:
    - path: "app/services/recommend_service.py"
      provides: "recommend() 파이프라인 오케스트레이터"
      exports: ["recommend"]
    - path: "app/routers/recommend.py"
      provides: "POST /recommend (stub -> service 호출 교체)"
      contains: "from app.services.recommend_service import recommend"
  key_links:
    - from: "app/routers/recommend.py"
      to: "app/services/recommend_service.py"
      via: "await recommend(body, request, background_tasks)"
      pattern: "await recommend"
    - from: "app/services/recommend_service.py"
      to: "request.app.state"
      via: "클라이언트 인스턴스 접근"
      pattern: "request\\.app\\.state"
    - from: "app/services/recommend_service.py"
      to: "app/services/background_summary.py"
      via: "background_tasks.add_task(run_background_summary, ...)"
      pattern: "add_task.*run_background_summary"
    - from: "app/services/recommend_service.py"
      to: "app/prompts/recommend.py"
      via: "build_recommend_system_prompt(profile, context_text)"
      pattern: "build_recommend_system_prompt"
---

<objective>
/recommend 엔드포인트를 stub에서 실제 서비스 파이프라인으로 교체한다. Plan 02의 meal_service와 동일한 패턴을 따른다.

Purpose: WAS가 POST /recommend를 호출하면 Pinecone 맥락 검색 -> Gemini 추천 생성 -> 응답 반환 -> Background Summary 저장 전체 파이프라인이 동작한다.
Output: app/services/recommend_service.py, 수정된 app/routers/recommend.py, 유닛 테스트
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endpoints-and-memory/03-CONTEXT.md
@.planning/phases/03-endpoints-and-memory/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 인터페이스 -->

From app/services/background_summary.py (Plan 01 생성):
```python
async def run_background_summary(
    user_id: str,
    user_message: str,
    ai_response: str,
    gemini_client,
    embed_client,
    pinecone_client,
) -> None:
```

From app/prompts/recommend.py (Plan 01 확장):
```python
def build_recommend_system_prompt(user_profile: UserProfile, context_text: str = "이전 맥락: 없음") -> str:
```

<!-- Phase 2 클라이언트 인터페이스 -->

From app/clients/gemini.py:
```python
class GeminiClient:
    async def generate(self, system_prompt: str, user_content: str, response_schema: type) -> str:
```

From app/clients/embedding.py:
```python
class EmbeddingClient:
    async def embed(self, text: str) -> list[float]:
```

From app/clients/pinecone.py:
```python
class PineconeClient:
    async def upsert(self, user_id: str, vector: list[float], summary: str) -> str:
    async def search(self, user_id: str, vector: list[float], top_k: int = 3) -> list[dict]:
```

From app/schemas/recommend.py:
```python
class RecommendRequest(BaseModel):
    user_id: str
    user_profile: UserProfile
    user_instruction: str

class RecommendedExercise(BaseModel):
    name: str
    burn_calories: float

class RecommendedMeal(BaseModel):
    name: str
    calories: float

class RecommendationData(BaseModel):
    recommended_exercise: RecommendedExercise
    recommended_meal: RecommendedMeal
```

From app/schemas/common.py:
```python
class SuccessResponse(BaseModel):
    status: str = "success"
    data: Any
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recommend_service.py 구현 + 유닛 테스트</name>
  <files>app/services/recommend_service.py, tests/test_recommend_service.py</files>
  <behavior>
    - test_recommend_passes_fields: body의 user_id, user_instruction이 올바르게 사용됨
    - test_recommend_pinecone_search: embed.embed(user_instruction) -> pinecone.search(user_id, vector, top_k=3) 순서로 호출
    - test_recommend_pinecone_failure: pinecone.search가 Exception raise해도 recommend는 정상 응답 반환
    - test_recommend_context_injection: 검색 결과가 있으면 "이전 맥락:\n1. ..." 형식으로 build_recommend_system_prompt에 전달
    - test_recommend_no_context: 검색 결과 빈 리스트일 때 "이전 맥락: 없음" 전달
    - test_recommend_gemini_call: gemini.generate(system_prompt, user_instruction, RecommendationData) 호출 확인
    - test_recommend_response_format: 반환값이 SuccessResponse(data={recommended_exercise: {name, burn_calories}, recommended_meal: {name, calories}})
    - test_recommend_gemini_failure: gemini.generate가 ClientError raise 시 HTTPException(500) with GEMINI_ERROR
    - test_recommend_background_task: background_tasks.add_task(run_background_summary, ...) 호출 확인. user_message 인자에 body.user_instruction 전달
  </behavior>
  <action>
    1. app/services/recommend_service.py 생성:
       - recommend(body: RecommendRequest, request: Request, background_tasks: BackgroundTasks) -> SuccessResponse
       - meal_service.py와 동일한 패턴:
         - 클라이언트 접근: request.app.state.gemini_client / pinecone_client / embed_client
         - _fetch_context(pinecone, embed, user_id, body.user_instruction) -> str (graceful degradation)
         - build_recommend_system_prompt(body.user_profile, context_text)
         - gemini.generate(prompt, body.user_instruction, RecommendationData)
         - Gemini 실패: HTTPException(500, GEMINI_ERROR)
         - json.loads -> RecommendationData(**parsed)
         - background_tasks.add_task(run_background_summary, user_id=body.user_id, user_message=body.user_instruction, ai_response=str(data.model_dump()), ...)
         - return SuccessResponse(data=data.model_dump())
       - 참고: _fetch_context 헬퍼가 meal_service.py와 중복되므로, 공용 유틸로 추출하거나 각 서비스에 인라인. Claude's Discretion에 따라 판단.

    2. tests/test_recommend_service.py 생성:
       - test_meal_service.py와 동일 mock 패턴
       - gemini_client.generate 반환값: '{"recommended_exercise": {"name": "조깅", "burn_calories": 300.0}, "recommended_meal": {"name": "샐러드", "calories": 400.0}}'
       - pytest.mark.anyio 사용
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_recommend_service.py -x -v</automated>
  </verify>
  <done>
    - recommend()가 전체 파이프라인을 실행
    - Pinecone 실패 시 graceful degradation
    - Gemini 실패 시 HTTPException(500, GEMINI_ERROR)
    - 9개 테스트 모두 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 라우터 교체 + 통합 테스트 추가</name>
  <files>app/routers/recommend.py, tests/test_endpoints.py</files>
  <action>
    1. app/routers/recommend.py 수정:
       - stub body 제거
       - from fastapi import APIRouter, BackgroundTasks, Request 추가
       - from app.services.recommend_service import recommend as handle_recommend
       - recommend_endpoint(body: RecommendRequest, request: Request, background_tasks: BackgroundTasks) -> SuccessResponse
       - return await handle_recommend(body, request, background_tasks)

    2. tests/test_endpoints.py에 추가 (Plan 02에서 생성된 파일에 추가):
       - test_recommend_calls_service: TestClient로 POST /recommend 호출, 서비스 레이어를 mock하여 200 응답 확인
       - test_recommend_422_invalid_body: 필수 필드 누락 시 422 반환
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_endpoints.py tests/test_recommend_service.py -x -v</automated>
  </verify>
  <done>
    - POST /recommend가 stub 대신 실제 서비스를 호출
    - 잘못된 요청 시 422 반환
    - Plan 02의 meal 통합 테스트와 recommend 통합 테스트 모두 통과
  </done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_recommend_service.py tests/test_endpoints.py -x -v 전체 통과
- python -m pytest tests/ -x 로 기존 테스트 포함 전체 통과
- POST /recommend가 stub이 아닌 서비스 파이프라인을 호출
</verification>

<success_criteria>
- POST /recommend에 유효한 요청 시 {status: "success", data: {recommended_exercise: {name, burn_calories}, recommended_meal: {name, calories}}} 반환
- Pinecone 검색 실패 시에도 정상 응답 반환
- Gemini 실패 시 500 + GEMINI_ERROR 반환
- 응답 후 Background Summary가 BackgroundTasks에 등록됨
</success_criteria>

<output>
After completion, create `.planning/phases/03-endpoints-and-memory/03-03-SUMMARY.md`
</output>
