---
phase: 06-8-mode-gemini-handlers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/gemini_outputs.py
  - app/schemas/chat.py
  - app/services/chat_service.py
autonomous: true
requirements: [CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07, CHAT-08, CHAT-09, CHAT-10]

must_haves:
  truths:
    - "_get_worker_response_schema(mode)가 모드별 고유 Pydantic 스키마를 반환한다"
    - "모드 1 Gemini 호출 시 answer 필드를 포함한 JSON이 반환된다"
    - "모드 2/3은 운동 계획 배열, 모드 4/5는 식단 배열 스키마를 사용한다"
    - "모드 6은 updated_fields 딕셔너리, 모드 7은 칼로리/영양소, 모드 8은 추천 배열 스키마를 사용한다"
    - "Gemini JSON 응답이 AiChatResponse.data에 모드별로 올바르게 매핑된다"
  artifacts:
    - path: "app/schemas/gemini_outputs.py"
      provides: "8개 모드별 Gemini 응답 Pydantic 스키마"
      exports: ["SimpleAnswerOutput", "ExercisePlanOutput", "MealPlanOutput", "UserDbUpdateOutput", "MealLogOutput", "RecommendationOutput"]
    - path: "app/services/chat_service.py"
      provides: "_get_worker_response_schema() 모드별 분기 + _build_ai_chat_data() 모드별 파싱"
      contains: "_get_worker_response_schema"
    - path: "app/schemas/chat.py"
      provides: "AiChatData에 detail 필드 추가"
      contains: "detail"
  key_links:
    - from: "app/services/chat_service.py"
      to: "app/schemas/gemini_outputs.py"
      via: "import in _get_worker_response_schema()"
      pattern: "from app\\.schemas\\.gemini_outputs import"
    - from: "app/services/chat_service.py"
      to: "app/schemas/chat.py"
      via: "AiChatData 구성"
      pattern: "AiChatData\\("
---

<objective>
8개 모드 각각에 대해 Gemini structured output용 Pydantic 스키마를 정의하고, chat_service.py의 _get_worker_response_schema()와 응답 파싱 로직을 모드별로 분기시킨다.

Purpose: Phase 5에서 SimpleAnswerOutput 단일 스키마를 사용하던 것을 모드별 전용 스키마로 교체하여, Gemini가 각 모드에 맞는 정형화된 JSON을 반환하도록 한다.
Output: app/schemas/gemini_outputs.py (8모드 스키마), chat_service.py 업데이트 (스키마 분기 + 파싱), chat.py 업데이트 (detail 필드)
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chat-pipeline-core/05-01-SUMMARY.md

@app/services/chat_service.py
@app/schemas/chat.py
@app/prompts/worker.py
@app/clients/gemini.py

<interfaces>
<!-- Gemini client accepts response_schema: type (Pydantic BaseModel subclass) -->
From app/clients/gemini.py:
```python
async def generate(self, system_prompt: str, user_content: str, response_schema: type) -> str:
    # returns JSON text string
```

From app/schemas/chat.py:
```python
class PlanItem(BaseModel):
    type: str; detail: str; value: str

class PlanData(BaseModel):
    date: str; items: list[PlanItem]

class DbUpdate(BaseModel):
    field: str; new_value: Any

class AiChatData(BaseModel):
    message: str
    plan: Optional[PlanData] = None
    db_update: Optional[DbUpdate] = None

class AiChatResponse(BaseModel):
    status: str = "success"
    mode: int
    data: AiChatData
    db_modified_flag: DbModifiedFlag
```

From app/prompts/worker.py _MODE_INSTRUCTIONS:
- Mode 1: {"answer": "..."}
- Mode 2: [{"date","type","detail","count"}]  (exercise plan)
- Mode 3: [{"date","type","detail","count"}]  (exercise plan modification)
- Mode 4: [{"date","food","time"}]  (meal plan)
- Mode 5: [{"date","food","time"}]  (meal plan modification)
- Mode 6: {"updated_fields": {"field_name": "value"}}
- Mode 7: {"calories": N, "carbs": N, "protein": N, "fat": N, "message": "..."}
- Mode 8: {"recommended_exercises": [{"name","calories"}], "recommended_meals": [{"name","calories"}]}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 8모드 Gemini 응답 스키마 정의 + AiChatData.detail 필드 추가</name>
  <files>app/schemas/gemini_outputs.py, app/schemas/chat.py, tests/test_gemini_outputs.py</files>
  <behavior>
    - SimpleAnswerOutput(answer: str)이 모드 1용 스키마로 존재한다
    - ExercisePlanItem(date, type, detail, count) 모델이 존재하고 ExercisePlanOutput은 list wrapper이다
    - MealPlanItem(date, food, time) 모델이 존재하고 MealPlanOutput은 list wrapper이다
    - UserDbUpdateOutput(updated_fields: dict[str, Any])이 존재한다
    - MealLogOutput(calories, carbs, protein, fat, message)이 존재한다
    - RecommendationExercise(name, calories), RecommendationMeal(name, calories), RecommendationOutput(recommended_exercises, recommended_meals)이 존재한다
    - AiChatData.detail 필드가 Optional[Any]로 추가되어 모드별 구조화 데이터를 담는다
  </behavior>
  <action>
1. app/schemas/gemini_outputs.py 생성 — 모드별 Gemini structured output 스키마 정의:
   - `SimpleAnswerOutput(answer: str)` — 모드 1 (chat_service.py에서 이동)
   - `ExercisePlanItem(date: str, type: str, detail: str, count: str)` + `ExercisePlanOutput(items: list[ExercisePlanItem])` — 모드 2, 3
     - 주의: Gemini structured output은 top-level list를 지원하지 않으므로 items wrapper 사용
   - `MealPlanItem(date: str, food: str, time: str)` + `MealPlanOutput(items: list[MealPlanItem])` — 모드 4, 5
   - `UserDbUpdateOutput(updated_fields: dict[str, Any])` — 모드 6
   - `MealLogOutput(calories: int, carbs: int, protein: int, fat: int, message: str)` — 모드 7
   - `RecommendationExercise(name: str, calories: int)`, `RecommendationMeal(name: str, calories: int)`, `RecommendationOutput(recommended_exercises: list[RecommendationExercise], recommended_meals: list[RecommendationMeal])` — 모드 8

2. app/schemas/chat.py 수정:
   - `AiChatData`에 `detail: Optional[Any] = None` 필드 추가 (모드별 구조화 JSON 데이터 전달용)
   - 기존 `plan`, `db_update` 필드는 유지 (하위 호환)

3. tests/test_gemini_outputs.py 생성 — 각 스키마 모델 검증:
   - 각 모델이 올바른 필드로 인스턴스화되는지 테스트
   - JSON 직렬화/역직렬화 라운드트립 확인

주의: Gemini `response_schema` 파라미터는 Pydantic BaseModel subclass를 받는다. top-level list는 불가하므로 `items: list[...]` wrapper 패턴 사용.
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_gemini_outputs.py -x -v</automated>
  </verify>
  <done>
    - gemini_outputs.py에 6개 출력 스키마(+3개 하위 모델)가 정의됨
    - AiChatData에 detail 필드가 추가됨
    - 모든 스키마 테스트 통과
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _get_worker_response_schema() 모드별 분기 + 응답 파싱 로직 업데이트</name>
  <files>app/services/chat_service.py, tests/test_chat_service.py</files>
  <behavior>
    - _get_worker_response_schema(1)은 SimpleAnswerOutput을 반환한다
    - _get_worker_response_schema(2)와 _get_worker_response_schema(3)은 ExercisePlanOutput을 반환한다
    - _get_worker_response_schema(4)와 _get_worker_response_schema(5)은 MealPlanOutput을 반환한다
    - _get_worker_response_schema(6)은 UserDbUpdateOutput을 반환한다
    - _get_worker_response_schema(7)은 MealLogOutput을 반환한다
    - _get_worker_response_schema(8)은 RecommendationOutput을 반환한다
    - 모드 1 응답 시 AiChatData.message에 answer 값이 들어간다
    - 모드 2 응답 시 AiChatData.detail에 운동 계획 배열이 들어간다
    - 모드 6 응답 시 AiChatData.detail에 updated_fields가 들어간다
    - 모드 7 응답 시 AiChatData.message에 message, AiChatData.detail에 영양소 데이터가 들어간다
    - 모드 8 응답 시 AiChatData.detail에 추천 배열이 들어간다
  </behavior>
  <action>
1. app/services/chat_service.py 수정:

   a. 상단 import에서 SimpleAnswerOutput 로컬 클래스 삭제하고 gemini_outputs에서 import:
      ```python
      from app.schemas.gemini_outputs import (
          SimpleAnswerOutput, ExercisePlanOutput, MealPlanOutput,
          UserDbUpdateOutput, MealLogOutput, RecommendationOutput,
      )
      ```

   b. `_get_worker_response_schema(mode)` 업데이트:
      ```python
      _MODE_SCHEMA_MAP: dict[int, type] = {
          1: SimpleAnswerOutput,
          2: ExercisePlanOutput,
          3: ExercisePlanOutput,
          4: MealPlanOutput,
          5: MealPlanOutput,
          6: UserDbUpdateOutput,
          7: MealLogOutput,
          8: RecommendationOutput,
      }

      def _get_worker_response_schema(mode: int) -> type:
          return _MODE_SCHEMA_MAP.get(mode, SimpleAnswerOutput)
      ```

   c. 새 헬퍼 `_build_ai_chat_data(mode, parsed)` 추가 — 모드별 AiChatData 구성:
      - 모드 1: message=parsed["answer"], detail=None
      - 모드 2, 3: message="운동 계획이 생성되었습니다." (또는 "수정"), detail=parsed["items"]
      - 모드 4, 5: message="식단 계획이 생성되었습니다." (또는 "수정"), detail=parsed["items"]
      - 모드 6: message="프로필이 업데이트되었습니다.", detail=parsed["updated_fields"]
      - 모드 7: message=parsed["message"], detail={"calories": ..., "carbs": ..., "protein": ..., "fat": ...}
      - 모드 8: message="추천 운동과 식단입니다.", detail=parsed

   d. handle_ai_chat() 라인 207-208 교체:
      기존: `ai_message: str = parsed.get("answer", raw_json)`
      신규: `chat_data = _build_ai_chat_data(mode, parsed)` 후 AiChatResponse에 직접 전달

2. tests/test_chat_service.py 수정:
   - _get_worker_response_schema 모드별 반환값 테스트 8개 추가
   - _build_ai_chat_data 모드별 파싱 테스트 추가 (모드 1, 2, 6, 7, 8 각 1개)
   - 기존 handle_ai_chat 테스트의 mock 응답을 SimpleAnswerOutput JSON 형태로 유지 (mode=1 fallback이므로)
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_chat_service.py tests/test_gemini_outputs.py -x -v</automated>
  </verify>
  <done>
    - _get_worker_response_schema()가 8개 모드 전체에 올바른 스키마 반환
    - _build_ai_chat_data()가 모드별로 AiChatData를 올바르게 구성
    - handle_ai_chat()가 모드별 파싱된 AiChatData를 응답에 포함
    - 기존 테스트 + 신규 테스트 모두 통과
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_gemini_outputs.py tests/test_chat_service.py -x -v` — 전체 테스트 통과
2. `python -c "from app.schemas.gemini_outputs import SimpleAnswerOutput, ExercisePlanOutput, MealPlanOutput, UserDbUpdateOutput, MealLogOutput, RecommendationOutput; print('All schemas importable')"` — 스키마 임포트 확인
3. `python -c "from app.services.chat_service import _get_worker_response_schema; schemas = [_get_worker_response_schema(m) for m in range(1,9)]; print([s.__name__ for s in schemas])"` — 모드별 스키마 매핑 확인
</verification>

<success_criteria>
- 8개 모드 각각에 대해 고유한 Gemini 응답 스키마가 정의되어 있다
- _get_worker_response_schema()가 모드별로 올바른 스키마를 반환한다
- Gemini JSON 응답이 모드별로 AiChatData(message, detail)에 올바르게 매핑된다
- AiChatResponse에 구조화된 detail 데이터가 포함된다
- 기존 테스트가 깨지지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/06-8-mode-gemini-handlers/06-01-SUMMARY.md`
</output>
