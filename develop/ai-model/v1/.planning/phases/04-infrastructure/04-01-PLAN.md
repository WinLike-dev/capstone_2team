---
phase: 04-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/exceptions.py
  - app/core/middleware.py
  - app/main.py
  - app/schemas/common.py
  - tests/test_error_handler.py
  - tests/test_middleware.py
autonomous: true
requirements: [ERR-01, ERR-02]

must_haves:
  truths:
    - "모든 예외가 구조화된 JSON 에러 응답으로 반환된다 (status, error_code, message 필드 포함)"
    - "모든 요청에 요청 ID, 처리 시간이 로그에 기록된다"
    - "HTTPException도 동일한 구조화 JSON 포맷으로 반환된다"
    - "예상치 못한 예외 발생 시 500 INTERNAL_ERROR가 구조화 JSON으로 반환된다"
  artifacts:
    - path: "app/core/exceptions.py"
      provides: "Custom exception classes with error_code mapping"
      exports: ["AppError", "NotFoundError", "ValidationError", "ExternalServiceError"]
    - path: "app/core/middleware.py"
      provides: "Request logging middleware with request_id and timing"
      exports: ["RequestLoggingMiddleware"]
    - path: "app/schemas/common.py"
      provides: "Updated ErrorResponse with error_code field"
      contains: "ErrorDetail"
  key_links:
    - from: "app/core/middleware.py"
      to: "app/main.py"
      via: "app.add_middleware()"
      pattern: "add_middleware.*RequestLogging"
    - from: "app/core/exceptions.py"
      to: "app/main.py"
      via: "exception_handler registration"
      pattern: "exception_handler.*AppError"
---

<objective>
글로벌 에러 핸들러를 고도화하고 구조화 로깅 미들웨어를 추가한다.

Purpose: Phase 5-6의 /ai-chat 파이프라인이 일관된 에러 응답 형식과 요청 추적을 안전하게 의존할 수 있도록 인프라를 구축한다.
Output: 커스텀 예외 클래스, 구조화 에러 핸들러, 요청 로깅 미들웨어
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/main.py
@app/schemas/common.py
@app/core/config.py
</context>

<interfaces>
<!-- Existing types the executor needs -->

From app/schemas/common.py:
```python
class ErrorDetail(BaseModel):
    code: str
    message: str

class ErrorResponse(BaseModel):
    status: str = "error"
    error: ErrorDetail
```

From app/main.py:
```python
@app.exception_handler(Exception)
async def global_exception_handler(request: FastAPIRequest, exc: Exception) -> JSONResponse:
    # Currently returns {"status": "error", "error": {"code": "INTERNAL_ERROR", "message": "..."}}
```

From app/core/config.py:
```python
class Settings(BaseSettings):
    LOG_LEVEL: str = "INFO"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Custom exceptions + structured error handlers</name>
  <files>app/core/exceptions.py, app/main.py, app/schemas/common.py, tests/test_error_handler.py</files>
  <behavior>
    - AppError(status_code=400, error_code="BAD_REQUEST", message="...") raises -> JSON {"status":"error","error":{"code":"BAD_REQUEST","message":"..."}} with 400 status
    - ExternalServiceError(service="WAS", message="timeout") raises -> JSON {"status":"error","error":{"code":"EXTERNAL_SERVICE_ERROR","message":"WAS: timeout"}} with 502 status
    - NotFoundError(message="...") raises -> JSON with 404 + NOT_FOUND code
    - ValidationError(message="...") raises -> JSON with 422 + VALIDATION_ERROR code
    - FastAPI HTTPException(status_code=404) -> same structured JSON format (not FastAPI default)
    - Unhandled Exception -> 500 + INTERNAL_ERROR (existing behavior preserved)
    - Error responses never leak stack traces or internal details in production
  </behavior>
  <action>
    1. Create app/core/exceptions.py with base AppError(Exception) class:
       - Fields: status_code (int), error_code (str), message (str)
       - Subclasses: NotFoundError(404, "NOT_FOUND"), ValidationError(422, "VALIDATION_ERROR"), ExternalServiceError(502, "EXTERNAL_SERVICE_ERROR")
       - ExternalServiceError takes service name and prepends it to message

    2. Update app/main.py:
       - Import AppError from app.core.exceptions
       - Add @app.exception_handler(AppError) that returns JSONResponse with {"status":"error","error":{"code": exc.error_code, "message": exc.message}}
       - Add @app.exception_handler(HTTPException) for FastAPI's HTTPException -> same structured format, mapping status_code to appropriate error_code
       - Keep existing global Exception handler for unhandled errors, but ensure it does NOT leak traceback in response (log it, return generic message)

    3. app/schemas/common.py: No schema changes needed - ErrorDetail(code, message) and ErrorResponse already match the target format.

    4. Write tests in tests/test_error_handler.py:
       - Test AppError subclass -> correct status code + JSON structure
       - Test HTTPException -> structured JSON (not FastAPI default HTML)
       - Test unhandled Exception -> 500 + INTERNAL_ERROR
       - Use a temporary test route that raises each exception type
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_error_handler.py -x -v</automated>
  </verify>
  <done>All custom exception types return structured JSON with status/error/code/message fields. HTTPException also returns structured format. No stack traces leak in error responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Request logging middleware with request_id and timing</name>
  <files>app/core/middleware.py, app/main.py, tests/test_middleware.py</files>
  <behavior>
    - Every request gets a unique request_id (UUID4) added to request.state
    - Response headers include X-Request-ID
    - Log entry on request completion includes: request_id, method, path, status_code, duration_ms
    - Log format is structured (key=value pairs for parseability)
    - request_id is accessible from request.state.request_id in route handlers
  </behavior>
  <action>
    1. Create app/core/middleware.py with RequestLoggingMiddleware(BaseHTTPMiddleware):
       - In dispatch(): generate uuid4 request_id, store in request.state.request_id
       - Record start_time before calling response = await call_next(request)
       - Calculate duration_ms = (time.time() - start_time) * 1000
       - Log at INFO level: "request_id={id} method={method} path={path} status={status} duration_ms={duration:.1f}"
       - Add X-Request-ID header to response
       - On exception: log at ERROR level with same fields + error message, re-raise

    2. Update app/main.py:
       - Import RequestLoggingMiddleware from app.core.middleware
       - Add app.add_middleware(RequestLoggingMiddleware) BEFORE routers are included
       - Configure logging.basicConfig with structlog-compatible format using settings.LOG_LEVEL

    3. Write tests in tests/test_middleware.py:
       - Test that GET /health response has X-Request-ID header (UUID format)
       - Test that request.state.request_id is set (create test route that returns it)
       - Test that log output contains request_id, method, path, status, duration_ms (use caplog fixture)
       - Test that error responses also get X-Request-ID
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_middleware.py -x -v</automated>
  </verify>
  <done>All requests get unique request_id in state and X-Request-ID header. Structured log entries contain request_id, method, path, status_code, duration_ms for every request.</done>
</task>

</tasks>

<verification>
cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_error_handler.py tests/test_middleware.py -x -v
</verification>

<success_criteria>
1. Custom AppError subclasses return structured JSON error responses with correct status codes
2. HTTPException returns structured JSON (not FastAPI default)
3. Unhandled exceptions return 500 INTERNAL_ERROR without leaking stack traces
4. Every request has X-Request-ID header with UUID4 value
5. Log entries contain request_id, method, path, status_code, duration_ms
6. All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-infrastructure/04-01-SUMMARY.md`
</output>
