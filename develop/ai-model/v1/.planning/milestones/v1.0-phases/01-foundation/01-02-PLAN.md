---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/schemas/__init__.py
  - app/schemas/common.py
  - app/schemas/meal.py
  - app/schemas/recommend.py
  - app/routers/meal.py
  - app/routers/recommend.py
  - app/main.py
  - tests/test_schemas.py
  - tests/test_stub_endpoints.py
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "/process-meal 스텁 엔드포인트가 유효한 요청에 성공 응답을 반환한다"
    - "/recommend 스텁 엔드포인트가 유효한 요청에 성공 응답을 반환한다"
    - "잘못된 요청 본문에 422 에러를 반환한다"
    - "Pydantic 스키마가 docs/DataFormat_2_ai.md의 JSON 규격과 일치한다"
  artifacts:
    - path: "app/schemas/common.py"
      provides: "공통 응답 envelope, UserProfile 모델"
      exports: ["SuccessResponse", "ErrorDetail", "ErrorResponse", "UserProfile"]
    - path: "app/schemas/meal.py"
      provides: "/process-meal 요청/응답 스키마"
      exports: ["ProcessMealRequest", "MealAnalysisData"]
    - path: "app/schemas/recommend.py"
      provides: "/recommend 요청/응답 스키마"
      exports: ["RecommendRequest", "RecommendedExercise", "RecommendedMeal", "RecommendationData"]
    - path: "app/routers/meal.py"
      provides: "/process-meal 스텁 엔드포인트"
      exports: ["router"]
    - path: "app/routers/recommend.py"
      provides: "/recommend 스텁 엔드포인트"
      exports: ["router"]
  key_links:
    - from: "app/routers/meal.py"
      to: "app/schemas/meal.py"
      via: "import request/response models"
      pattern: "from app\\.schemas\\.meal import"
    - from: "app/routers/recommend.py"
      to: "app/schemas/recommend.py"
      via: "import request/response models"
      pattern: "from app\\.schemas\\.recommend import"
    - from: "app/main.py"
      to: "app/routers/meal.py"
      via: "app.include_router"
      pattern: "include_router.*meal"
    - from: "app/main.py"
      to: "app/routers/recommend.py"
      via: "app.include_router"
      pattern: "include_router.*recommend"
---

<objective>
WAS와의 API 계약을 Pydantic 스키마로 코드화하고, /process-meal과 /recommend 스텁 엔드포인트를 구현한다.

Purpose: WAS 팀이 즉시 연동 테스트를 시작할 수 있는 API 계약 확정. Phase 3에서 스텁 내부만 실제 로직으로 교체.
Output: 요청/응답 Pydantic 모델, 스텁 엔드포인트 2개, 스키마/엔드포인트 테스트
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 핵심 인터페이스. Executor가 직접 사용. -->

From app/core/config.py:
```python
from pydantic_settings import BaseSettings
from functools import lru_cache

class Settings(BaseSettings):
    GEMINI_API_KEY: str
    PINECONE_API_KEY: str
    PINECONE_INDEX_NAME: str
    WAS_BASE_URL: str
    APP_ENV: str = "development"
    LOG_LEVEL: str = "INFO"

@lru_cache
def get_settings() -> Settings: ...
```

From app/main.py:
```python
from fastapi import FastAPI
app = FastAPI(title="AI Hub", lifespan=lifespan)
# Plan 02에서 include_router 추가 필요
```
</interfaces>

<api_spec>
<!-- docs/DataFormat_2_ai.md에서 추출한 JSON 규격. 스키마 정의의 원천. -->

POST /process-meal 요청:
```json
{
  "user_id": "string",
  "user_profile": {
    "gender": "string", "age": "number", "bmi": "number",
    "goal": "string", "medical_history": ["string"], "allergies": ["string"]
  },
  "user_instruction": "string",
  "user_message": "string"
}
```

POST /process-meal 응답:
```json
{"status": "success", "data": {"calories": "number", "message": "string"}}
```

POST /recommend 요청:
```json
{
  "user_id": "string",
  "user_profile": {
    "gender": "string", "age": "number", "bmi": "number",
    "goal": "string", "activity_level": "string"
  },
  "user_instruction": "string"
}
```

POST /recommend 응답:
```json
{
  "status": "success",
  "data": {
    "recommended_exercise": {"name": "string", "burn_calories": "number"},
    "recommended_meal": {"name": "string", "calories": "number"}
  }
}
```

에러 응답:
```json
{"status": "error", "error": {"code": "ERROR_CODE", "message": "설명"}}
```
</api_spec>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic 스키마 정의 (common, meal, recommend)</name>
  <files>app/schemas/__init__.py, app/schemas/common.py, app/schemas/meal.py, app/schemas/recommend.py, tests/test_schemas.py</files>
  <action>
1. **app/schemas/common.py** — 공통 모델:
   ```python
   from pydantic import BaseModel
   from typing import Optional, Any

   class UserProfile(BaseModel):
       """통합 UserProfile — 모든 필드 Optional, 엔드포인트별로 공유."""
       gender: Optional[str] = None
       age: Optional[int] = None
       bmi: Optional[float] = None
       goal: Optional[str] = None
       medical_history: Optional[list[str]] = None
       allergies: Optional[list[str]] = None
       activity_level: Optional[str] = None

   class ErrorDetail(BaseModel):
       code: str
       message: str

   class SuccessResponse(BaseModel):
       status: str = "success"
       data: Any

   class ErrorResponse(BaseModel):
       status: str = "error"
       error: ErrorDetail
   ```
   UserProfile은 통합 모델 — per user decision (모든 필드 Optional, /process-meal은 medical_history/allergies 사용, /recommend는 activity_level 사용).

2. **app/schemas/meal.py** — /process-meal 스키마:
   ```python
   from pydantic import BaseModel
   from app.schemas.common import UserProfile

   class ProcessMealRequest(BaseModel):
       user_id: str
       user_profile: UserProfile
       user_instruction: str
       user_message: str

   class MealAnalysisData(BaseModel):
       calories: float
       message: str
   ```

3. **app/schemas/recommend.py** — /recommend 스키마:
   ```python
   from pydantic import BaseModel
   from app.schemas.common import UserProfile

   class RecommendRequest(BaseModel):
       user_id: str
       user_profile: UserProfile
       user_instruction: str

   class RecommendedExercise(BaseModel):
       name: str
       burn_calories: float

   class RecommendedMeal(BaseModel):
       name: str
       calories: float

   class RecommendationData(BaseModel):
       recommended_exercise: RecommendedExercise
       recommended_meal: RecommendedMeal
   ```

4. **app/schemas/__init__.py** — re-export:
   ```python
   from app.schemas.common import UserProfile, SuccessResponse, ErrorResponse, ErrorDetail
   from app.schemas.meal import ProcessMealRequest, MealAnalysisData
   from app.schemas.recommend import RecommendRequest, RecommendationData, RecommendedExercise, RecommendedMeal
   ```

5. **tests/test_schemas.py** — 스키마 검증 테스트:
   - ProcessMealRequest가 유효한 JSON을 파싱
   - ProcessMealRequest가 user_id 누락 시 ValidationError
   - RecommendRequest가 유효한 JSON을 파싱
   - UserProfile의 Optional 필드가 None 기본값
   - MealAnalysisData, RecommendationData 직렬화 확인
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_schemas.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>Pydantic 스키마가 docs/DataFormat_2_ai.md JSON 규격과 일치하고, 유효성 검증 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: 스텁 엔드포인트 + 라우터 등록</name>
  <files>app/routers/meal.py, app/routers/recommend.py, app/main.py, tests/test_stub_endpoints.py</files>
  <action>
1. **app/routers/meal.py** — /process-meal 스텁:
   ```python
   from fastapi import APIRouter
   from app.schemas.common import SuccessResponse
   from app.schemas.meal import ProcessMealRequest, MealAnalysisData

   router = APIRouter(tags=["meal"])

   @router.post("/process-meal", response_model=SuccessResponse)
   async def process_meal(request: ProcessMealRequest):
       # 스텁: Phase 3에서 실제 Gemini 호출로 교체
       stub_data = MealAnalysisData(
           calories=350.0,
           message="식단이 기록되었습니다. 균형 잡힌 식사입니다."
       )
       return SuccessResponse(data=stub_data.model_dump())
   ```

2. **app/routers/recommend.py** — /recommend 스텁:
   ```python
   from fastapi import APIRouter
   from app.schemas.common import SuccessResponse
   from app.schemas.recommend import RecommendRequest, RecommendationData, RecommendedExercise, RecommendedMeal

   router = APIRouter(tags=["recommend"])

   @router.post("/recommend", response_model=SuccessResponse)
   async def recommend(request: RecommendRequest):
       # 스텁: Phase 3에서 실제 Gemini 호출로 교체
       stub_data = RecommendationData(
           recommended_exercise=RecommendedExercise(name="조깅", burn_calories=300.0),
           recommended_meal=RecommendedMeal(name="닭가슴살 샐러드", calories=400.0)
       )
       return SuccessResponse(data=stub_data.model_dump())
   ```

3. **app/main.py 수정** — 라우터 include 추가:
   기존 코드에 아래 추가:
   ```python
   from app.routers.meal import router as meal_router
   from app.routers.recommend import router as recommend_router

   app.include_router(meal_router)
   app.include_router(recommend_router)
   ```

4. **tests/test_stub_endpoints.py** — 엔드포인트 통합 테스트:
   httpx AsyncClient + ASGITransport 사용.
   - test_process_meal_valid_request: 유효한 요청 → 200 + {"status": "success", "data": {"calories": ..., "message": ...}}
   - test_process_meal_missing_field: user_id 누락 → 422
   - test_recommend_valid_request: 유효한 요청 → 200 + {"status": "success", "data": {"recommended_exercise": ..., "recommended_meal": ...}}
   - test_recommend_missing_field: user_id 누락 → 422
   - test_process_meal_empty_profile: user_profile 필드가 최소한만 있어도 성공 (Optional 필드 검증)
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_stub_endpoints.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>/process-meal, /recommend 스텁이 유효한 요청에 성공 응답, 잘못된 요청에 422를 반환한다</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v` → 전체 테스트 통과 (health + schemas + stub endpoints)
2. `uvicorn app.main:app` 실행 후:
   - `curl http://localhost:8000/health` → 200
   - `curl -X POST http://localhost:8000/process-meal -H "Content-Type: application/json" -d '{"user_id":"u1","user_profile":{},"user_instruction":"test","user_message":"점심 먹었어"}'` → 200 + 성공 응답
   - `curl -X POST http://localhost:8000/recommend -H "Content-Type: application/json" -d '{"user_id":"u1","user_profile":{},"user_instruction":"추천해줘"}'` → 200 + 성공 응답
   - `curl -X POST http://localhost:8000/process-meal -H "Content-Type: application/json" -d '{}'` → 422
3. `http://localhost:8000/docs` → Swagger UI에서 3개 엔드포인트 확인
</verification>

<success_criteria>
- Pydantic 스키마가 docs/DataFormat_2_ai.md의 JSON 규격과 정확히 일치
- /process-meal 스텁이 유효 요청에 200 + {status: "success", data: {calories, message}} 반환
- /recommend 스텁이 유효 요청에 200 + {status: "success", data: {recommended_exercise, recommended_meal}} 반환
- 잘못된 요청에 422 에러 반환
- 스키마 + 엔드포인트 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
