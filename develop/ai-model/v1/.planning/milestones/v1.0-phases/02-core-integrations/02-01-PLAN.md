---
phase: 02-core-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/config.py
  - requirements.txt
  - app/clients/embedding.py
  - tests/test_embedding.py
autonomous: true
requirements: [EMBD-01, EMBD-02, EMBD-03]

must_haves:
  truths:
    - "한국어 텍스트를 입력하면 384차원 float 리스트가 반환된다"
    - "임베딩 생성이 run_in_threadpool으로 event loop를 블록하지 않는다"
    - "Settings에 GEMINI_MODEL_NAME, ROUTER_API_KEY, ROUTER_MODEL_NAME 환경변수가 추가되어 있다"
  artifacts:
    - path: "app/clients/embedding.py"
      provides: "EmbeddingClient with async embed() method"
      exports: ["EmbeddingClient"]
    - path: "app/core/config.py"
      provides: "Updated Settings with new env vars"
      contains: "GEMINI_MODEL_NAME"
    - path: "tests/test_embedding.py"
      provides: "Unit tests for EmbeddingClient"
      min_lines: 20
  key_links:
    - from: "app/clients/embedding.py"
      to: "starlette.concurrency.run_in_threadpool"
      via: "CPU offload for encode()"
      pattern: "run_in_threadpool"
---

<objective>
Settings 환경변수 추가, 의존성 업데이트, EmbeddingClient 구현

Purpose: Phase 2의 모든 클라이언트가 필요로 하는 Settings 환경변수(GEMINI_MODEL_NAME, ROUTER_API_KEY, ROUTER_MODEL_NAME)를 추가하고, sentence-transformers 기반 EmbeddingClient를 구현한다. 임베딩 모델은 paraphrase-multilingual-MiniLM-L12-v2 (384-dim)이며, run_in_threadpool로 event loop 블록을 방지한다.

Output: 업데이트된 config.py, requirements.txt, EmbeddingClient 클래스, 단위 테스트
</objective>

<execution_context>
@C:/Users/ksh00/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksh00/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-integrations/02-CONTEXT.md
@.planning/phases/02-core-integrations/02-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs -->

From app/core/config.py:
```python
from functools import lru_cache
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    GEMINI_API_KEY: str
    PINECONE_API_KEY: str
    PINECONE_INDEX_NAME: str
    WAS_BASE_URL: str
    APP_ENV: str = "development"
    LOG_LEVEL: str = "INFO"
    model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}

@lru_cache
def get_settings() -> Settings:
    return Settings()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings 환경변수 추가 + 의존성 업데이트</name>
  <files>app/core/config.py, requirements.txt, .env.example</files>
  <action>
1. app/core/config.py의 Settings 클래스에 3개 필드 추가:
   - GEMINI_MODEL_NAME: str = "gemini-2.0-flash" (기본값 있음)
   - ROUTER_API_KEY: str (필수, 기본값 없음)
   - ROUTER_MODEL_NAME: str = "gemini-2.0-flash-lite" (기본값 있음)

2. requirements.txt에 Phase 2 의존성 추가:
   - pinecone[asyncio]>=8.0
   - sentence-transformers>=5.0
   - google-genai>=1.0
   - tenacity>=9.0

3. .env.example에 새 환경변수 템플릿 추가:
   - GEMINI_MODEL_NAME=gemini-2.0-flash
   - ROUTER_API_KEY=your-router-api-key
   - ROUTER_MODEL_NAME=gemini-2.0-flash-lite

기존 Settings 필드와 model_config, lru_cache 패턴은 유지한다.
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -c "import sys; sys.path.insert(0,'.'); from app.core.config import Settings; s = Settings.model_fields; assert 'GEMINI_MODEL_NAME' in s and 'ROUTER_API_KEY' in s and 'ROUTER_MODEL_NAME' in s; print('OK: all new fields present')"</automated>
  </verify>
  <done>Settings에 GEMINI_MODEL_NAME, ROUTER_API_KEY, ROUTER_MODEL_NAME 필드 존재. requirements.txt에 pinecone[asyncio], sentence-transformers, google-genai, tenacity 추가됨.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EmbeddingClient 구현 + 단위 테스트</name>
  <files>app/clients/embedding.py, tests/test_embedding.py</files>
  <behavior>
    - Test 1: embed("안녕하세요")가 384개 float 원소를 가진 list를 반환한다 (모킹된 모델)
    - Test 2: embed() 호출 시 run_in_threadpool이 사용된다 (패치로 확인)
    - Test 3: 빈 문자열 입력 시에도 384-dim 벡터가 반환된다
  </behavior>
  <action>
1. app/clients/embedding.py 생성:
   - EmbeddingClient 클래스 정의
   - __init__(self, model: SentenceTransformer) -> None: self._model = model 저장
   - async def embed(self, text: str) -> list[float]: run_in_threadpool(self._model.encode, text)로 벡터 생성, .tolist()로 변환하여 반환
   - EMBEDDING_DIM = 384 상수 정의 (Pinecone 인덱스와 일치 검증용)

CRITICAL: paraphrase-multilingual-MiniLM-L12-v2는 384-dim을 출력한다 (CONTEXT.md에 768이라 적혀 있으나 리서치에서 384로 확인됨). Pinecone 인덱스도 반드시 384로 생성해야 한다.

2. tests/test_embedding.py 생성:
   - SentenceTransformer를 AsyncMock/MagicMock으로 모킹 (encode가 np.zeros(384) 반환하도록)
   - run_in_threadpool을 패치하여 실제 threadpool 없이 테스트
   - @pytest.mark.anyio 데코레이터 사용
  </action>
  <verify>
    <automated>cd C:/Users/ksh00/anti_projects/capstone_2team/develop/ai-model/fastapi && python -m pytest tests/test_embedding.py -v</automated>
  </verify>
  <done>EmbeddingClient.embed()가 384-dim float list를 반환하며 run_in_threadpool을 사용한다. 3개 이상 테스트 통과.</done>
</task>

</tasks>

<verification>
- Settings.model_fields에 GEMINI_MODEL_NAME, ROUTER_API_KEY, ROUTER_MODEL_NAME 존재
- requirements.txt에 4개 새 의존성 존재
- EmbeddingClient가 384-dim 벡터를 async로 반환
- 모든 테스트 통과: python -m pytest tests/test_embedding.py -v
</verification>

<success_criteria>
- Settings 환경변수 3개 추가 완료
- requirements.txt 의존성 4개 추가 완료
- EmbeddingClient.embed()가 384-dim list[float] 반환
- run_in_threadpool 사용으로 event loop 블록 방지
- 단위 테스트 3개 이상 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-integrations/02-01-SUMMARY.md`
</output>
