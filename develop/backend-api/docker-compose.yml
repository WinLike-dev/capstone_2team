version: '3.8'

services:
  backend:
    build: .
    container_name: capstone-backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: always
    volumes:
      - ./logs:/usr/src/app/logs
